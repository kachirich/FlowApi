# Documentation images

Drop dashboard screenshots here for the README to pick them up:

- `dashboard.png`  — Live Dashboard view (Quick Actions, Recent Inbound Activity, Active API Keys, Lead Ledger).
- `api-keys.png`   — Active API Keys table with routing flow selector, above the Lead Vault (scored leads, delivery status, re-fire action).
- `analytics.png`  — Webhook Traffic Analytics page with the timestamps and status table.
- `pricing.png`    — Transparent Pricing page with the Sandbox / Growth / Enterprise tiers.

These paths are referenced from the top-level `README.md`.
