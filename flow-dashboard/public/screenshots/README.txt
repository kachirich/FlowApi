Drop the dashboard screenshots here for the landing page to pick them up:

  dashboard.png   - Live Dashboard view (Quick Actions, Recent Inbound Activity,
                    Active API Keys, Lead Ledger). Recommended 1366x768 or wider.
  api-keys.png    - Generate API Key panel expanded with the Active API Keys table.
  analytics.png   - Webhook Traffic Analytics page with the timestamps/status table.

These are referenced from src/pages/LandingPage.jsx as /screenshots/<name>.png.
Vite serves anything in public/ at the site root, so no import is required.
